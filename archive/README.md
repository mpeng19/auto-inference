# archive

Kept for reference, not called by the harness or the research loop.

- `modal_hello_world.py` — the original Modal smoke test. Superseded by
  `src/autoinf/modal_app.py::smoke`, which exercises the real path.
